import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Lagos State Military Recruitment EOI Portal',
  description: 'Expression of Interest portal for Lagos indigenes to register for Nigerian Military recruitment',
  keywords: ['Lagos', 'Military', 'Recruitment', 'Nigerian Army', 'Navy', 'Air Force'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
